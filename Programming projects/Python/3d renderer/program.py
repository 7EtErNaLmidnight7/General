# Use setup() to setup your code, this is at the start of every program
# end with end()
# See DOC for more info

from renderer import setup, end, point, line, cuboid
from turtle import *
import math

setup()
cuboid(0, 0, 0, 100, 200, 32, True)
end()